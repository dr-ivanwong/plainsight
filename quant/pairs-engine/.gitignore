# Virtual environment and caches
.venv/
__pycache__/
.pytest_cache/
*.egg-info/

# Operator-local data and outputs: the close cache and written artefacts
# are per-machine working state, never committed.
data/
artefacts/

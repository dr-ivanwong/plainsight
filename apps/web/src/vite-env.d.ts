/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** The app version, injected at build time from package.json (vite.config.ts define). */
declare const __APP_VERSION__: string;
